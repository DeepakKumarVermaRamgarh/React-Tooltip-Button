// Importing the CSS file for styling the App component.
import "./App.css";

import { Homepage } from "./pages/Homepage/HomePage";

function App() {
  // Rendering the Homepage component as the main content of the App component.
  return <Homepage />;
}

// Exporting the App component to make it available for use in other parts of the application.
export default App;
